{
  "name": "aetherdex-monorepo",
  "private": true,
  "scripts": {
    "dev": "bun --filter aether-dex dev",
    "web:dev": "bun --filter aether-dex dev",
    "lint": "bun --filter '*' lint",
    "test": "bun --filter '*' test",
    "typecheck": "bun --filter '*' typecheck",
    "build": "bun --filter '*' build"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "tooling/*"
  ],
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.0",
    "@vitest/ui": "^2.1.8",
    "vitest": "^2.1.8"
  }
}